import { StyleSheet } from 'react-native'

const globalStyles = StyleSheet.create({
  screenTitle: {
    textAlign: 'center',
    fontSize: 40
  },
  container: {
    flex: 1,
    paddingTop: 40
  }
})

export default globalStyles